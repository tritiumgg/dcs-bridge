//! `protoc-gen-dcsbridge-lua`, the DCS-Bridge protoc plugin.
//!
//! A stub. Phase 3 gives it the seven artefacts SPEC 8.3 lists, and the
//! `FEATURE_PROTO3_OPTIONAL` advertisement protoc requires before it will run
//! the plugin at all.

fn main() {
    println!("protoc-gen-dcsbridge-lua {}", env!("CARGO_PKG_VERSION"));
}
